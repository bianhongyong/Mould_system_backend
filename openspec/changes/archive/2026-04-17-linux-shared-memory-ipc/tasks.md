# Tasks

## 1. POSIX 共享内存数据面基础

- [x] 1.1 在 `common/comm` 新增共享内存段管理组件（命名、`shm_open`、`ftruncate`、`mmap`、unmap/close）
- [x] 1.2 定义通道映射初始化流程（create/attach、版本与 magic 校验、参数化容量）
- [x] 1.3 将单机默认数据面切换到 Linux SHM 路径，并彻底替换旧的进程内虚拟 SHM 路径
- [x] 1.4 执行并通过模块 1 单元测试；仅在通过后进入模块 2

## 2. 模块通道配置文件解析与汇总

- [x] 2.1 新增模块 `.txt` 通道配置解析器（输入通道、输出通道、可选参数）及语法校验
- [x] 2.2 实现全模块通道配置汇总器，生成 channel -> producers/consumers/count/params 拓扑索引
- [x] 2.3 基于汇总拓扑驱动共享内存段预分配（按消费者数量与参数计算）
- [x] 2.4 执行并通过模块 2 单元测试（配置解析、汇总正确性、冲突校验）；仅在通过后进入模块 3

## 3. Ring Buffer 布局与提交语义

- [x] 3.1 定义 `RingHeader`、`ConsumerTable`、`SlotMeta`、`PayloadRegion` 固定内存布局
- [x] 3.2 实现 slot 两阶段提交状态机（reserve/write/commit）与可见性内存序
- [x] 3.3 增加布局版本不兼容检测与拒绝附着逻辑
- [x] 3.4 执行并通过模块 3 单元测试；仅在通过后进入模块 4

## 4. 多消费者游标与回收

- [x] 4.1 实现每消费者独立读游标注册、推进与持久状态维护
- [x] 4.2 实现基于最小读序号的安全回收逻辑与容量返还
- [x] 4.3 实现慢消费者隔离与 lag 计算
- [x] 4.4 定义重复可判定键（`channel + sequence`）并在消费端接入幂等去重检查路径
- [x] 4.5 执行并通过模块 4 单元测试；仅在通过后进入模块 5

## 5. 守护进程消费者在线治理与可观测性

- [x] 5.1 在数据平面提供守护进程可调用的消费者在线控制接口（add/remove），并定义调用契约与参数校验
- [x] 5.2 实现 online set 的 generation 原子切换机制，支持消费者新增/删除且不要求暂停数据面读写（影响回收 quorum）
- [x] 5.3 定义消费者简化状态（`ONLINE/OFFLINE`）：新增消费者游标从 ring 最新 slot 开始；重新上线按新消费者处理，不追历史消息
- [x] 5.4 实现删除消费者后的最小游标重算与 slot 回收触发（必要时更新 slot 状态并立即触发一次回收）
- [x] 5.5 暴露控制面-共享内存联合健康指标（占用、阻塞时长、lag、回收次数、成员变更次数、上线/下线事件）
- [x] 5.6 执行并通过模块 5 单元测试（新增游标起点正确性、删除触发回收正确性、原子切换一致性、观测指标完整性）；仅在通过后进入模块 6

## 6. 跨进程同步与背压

- [x] 6.1 引入 `eventfd + epoll` 跨进程通知机制，并在 ring layout 中增加 `(channel, consumer) -> eventfd` 通知映射区域
- [x] 6.2 由守护进程在 `fork` 前创建通知 `eventfd` 并完成继承；发布成功后广播唤醒在线消费者，消费者通过 `epoll` 监听订阅通道并按 `read_sequence` 从共享内存消费
- [x] 6.3 `ShmPubSubBus` 内部 reactor 线程完成消息反序列化并通过订阅 handler 透传消息；`ModuleBase` 仅调用发布接口并维持主线程回调执行
- [x] 6.4 本阶段暂不实现完整背压治理（超时/阈值/失败分级）；先补齐发布等待与阻塞观测日志（开始/结束/时长/occupancy/lag）
- [x] 6.5 执行并通过模块 6 单元测试；本 change 结项