syntax = "proto3";
package mould.gateway.scenario;

// 表面测距场景 — 图片附属的物理尺寸参数
message SurfaceMetering {
  double length_mm = 1;    // 长度 (毫米)
  double width_mm = 2;     // 宽度 (毫米)
  double area_mm2 = 3;     // 面积 (平方毫米)
}
